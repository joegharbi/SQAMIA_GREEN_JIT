# ErlangServer results

This folder groups the checked-in outputs for the echo server case study.

- `raw/` contains direct run outputs from the measurement scripts.
- `summary/` contains merged or cleaned tables used as analysis inputs.
- `derived/` contains ratio, normalization, or comparison tables.

Keep published or historically important CSV files here even when they are no longer part of the active workflow.
