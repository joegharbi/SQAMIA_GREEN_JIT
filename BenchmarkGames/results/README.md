# BenchmarkGames results

This folder groups the checked-in outputs for the benchmark game study.

- `raw/` is for direct benchmark outputs.
- `summary/` holds curated tables that are used as analysis inputs.
- `derived/` holds normalized and ratio tables created from the summary data.

These files are kept in version control because they document historical experiment states and support reproducibility for published comparisons.
