# Erlang benchmark results

This folder stores the checked-in results for the Erlang benchmark game runs.

- `raw/` contains direct measurement outputs.
- `summary/` contains curated comparison tables.
- `derived/` contains ratios and averaged outputs built from the raw or summary files.

The files are retained because they document historical runs used in analysis and paper preparation.
