# RosettaQueens results

This folder stores the checked-in outputs for the RosettaQueens study.

- `raw/` keeps direct outputs from benchmark runs.
- `summary/` keeps averaged or curated tables used in later analysis.
- `derived/` keeps ratio files generated from the checked-in datasets.

The files are kept in version control because they document historical benchmark states used in publications and follow-up experiments.
